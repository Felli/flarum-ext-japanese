# .scrutinizer.yml

checks:
    php:
        code_rating: true
        duplication: true

build:
    nodes:
        tests: true
        analysis:
            tests:
                override:
                    -
                    - php-scrutinizer-run
 
